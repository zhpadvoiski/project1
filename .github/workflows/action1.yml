name: Pull Request

on:
  pull_request

jobs:
  pull-request:
    runs-on: ubuntu-latest
    steps:
      - name: Run command
        run: |
        echo "This is pull request from origin"
        ls -la
